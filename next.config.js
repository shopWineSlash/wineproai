/**
 * @type {import('next').NextConfig}
 *
 * The Next.js configuration for the WineProAI website. This file exports
 * a simple configuration object. You can extend this configuration
 * to enable features like custom webpack rules or environment variables.
 */
const nextConfig = {
  // React strict mode helps identify potential problems in your code during
  // development. It's enabled by default in newer Next.js projects. If you
  // experience unexpected behavior while developing, consider enabling it.
  reactStrictMode: true
};

module.exports = nextConfig;
